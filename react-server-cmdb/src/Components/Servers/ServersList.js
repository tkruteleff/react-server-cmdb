import React from 'react';

import ServerListItem from './ServersListItem';

const ServersList = (props) => {
    return (
        <div>
            <p>List servers here</p>
            <ServerListItem />
        </div>
    )
};

export default ServersList;